# Meshtastic-Telegram Bridge

Мост между Mesh-сетью Meshtastic и Telegram через MQTT.

## Описание

Этот проект позволяет связывать Mesh-сеть Meshtastic с Telegram, обеспечивая двустороннюю передачу сообщений, позиций и другой информации между участниками Mesh-сети и пользователями Telegram.

## Возможности

- 📡 **Двусторонняя передача сообщений** между Mesh-сетью и Telegram
- 📍 **Отправка и получение позиций** через Telegram
- 📊 **Статистика** узлов сети и сообщений
- 👥 **Управление пользователями** и правами доступа
- 🔋 **Мониторинг телеметрии** узлов (заряд батареи и др.)
- ⚙️ **Административные функции** для управления мостом

## Требования

- Python 3.11+
- MQTT брокер (Mosquitto или другой)
- Telegram Bot Token
- Meshtastic устройство, подключенное к MQTT

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd meshtastic_telegramm_bridge
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте конфигурацию:
```bash
cp config/config.yaml.example config/config.yaml
# Отредактируйте config/config.yaml и укажите ваш Telegram Bot Token
```

4. Создайте необходимые директории:
```bash
mkdir -p logs storage
```

## Конфигурация

Основные настройки в `config/config.yaml`:

- **MQTT**: адрес брокера, порт, учетные данные
- **Telegram**: токен бота, ID администраторов
- **Bridge**: формат сообщений, максимальная длина и т.д.

Подробнее см. `config/config.yaml.example`.

## Использование

### Запуск напрямую

```bash
python main.py
```

### Запуск через Docker

```bash
docker-compose up -d
```

## Структура проекта

```
meshtastic_telegramm_bridge/
├── src/
│   ├── __init__.py
│   ├── bridge.py          # Основной класс моста
│   ├── mqtt_client.py     # MQTT клиент для Meshtastic
│   ├── telegram_bot.py    # Telegram бот
│   └── models.py          # Модели базы данных
├── config/
│   └── config.yaml        # Конфигурация (создается вручную)
├── logs/                  # Логи приложения
├── storage/               # База данных SQLite
├── main.py                # Точка входа
├── requirements.txt       # Зависимости Python
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose конфигурация
└── mosquitto.conf         # Конфигурация MQTT брокера
```

## Команды Telegram бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/nodes` - Список активных узлов сети
- `/stats` - Статистика моста
- `/location` - Отправить ваше местоположение
- `/admin` - Панель администратора (только для админов)

## Лицензия

[Укажите вашу лицензию]

## Авторы

[Укажите авторов]

